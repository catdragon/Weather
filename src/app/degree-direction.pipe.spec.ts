import { DegreeDirectionPipe } from './degree-direction.pipe';

describe('DegreeDirectionPipe', () => {
  it('create an instance', () => {
    const pipe = new DegreeDirectionPipe();
    expect(pipe).toBeTruthy();
  });
});
